/* @ts-nocheck */
export default function Login(_props: { onLoginSuccess?: () => void; onSwitchToSignup?: () => void; }) {
  // Minimal stub for compile
  return <div className="p-4">Login (stub)</div>;
}